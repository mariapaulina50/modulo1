@model FoodHealthModulo1Final.Models.Diary.AppointmentType

@{
    ViewBag.Title = "Details";
}

<header class="masthead">
    <div class="container d-flex h-100 align-items-center">
        <div class="container-fluid rounded" style="background-color: #F5F5F5 ;">
            <h4 style="padding-top:80px">Detalles Tipo de Cita</h4>
            <br />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.NameAppointment)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.NameAppointment)
                </dd>

            </dl>
            <p>
                @Html.ActionLink("Editar", "Edit", new { id = Model.AppointmentTypeID }, new { @class = "btn btn-primary" }) |
                @Html.ActionLink("Volver a la Lista", "Index", new { }, new { @class = "btn btn-secondary" })
            </p>
            <br />
        </div>
    </div>
</header>